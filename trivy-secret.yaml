# Two literal synthetic fixtures retained in docs/launch-readiness/changes.patch
# (lines 411 and 624 at a453758). These are conspicuously fake test values, not
# usable credentials. Preserve the historical patch and its evidence manifest.
# Anchored exact-value exceptions only: no path, rule ID or key-prefix exemption.
# scripts/trivy-fixture-test.sh proves near-matches and other keys still fail.
allow-rules:
  - id: historical-mono-rotation-fixture
    description: Exact dummy value from the archived secret-rotation test
    regex: '^sk_live_verysecretkey1234$'
  - id: historical-paystack-rotation-fixture
    description: Exact dummy value from the archived endpoint test
    regex: '^sk_test_abc1234567890xyz$'
