# Local container administration; PostgreSQL has no published host port.
local all all trust
# Network connections must use TLS and password authentication.
hostnossl all all all reject
hostssl all all all scram-sha-256
