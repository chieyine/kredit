name: kredit-prod

services:
  postgres:
    image: postgres:18
    restart: unless-stopped
    environment:
      POSTGRES_DB: kredit
      POSTGRES_USER: kredit
      POSTGRES_PASSWORD: ${POSTGRES_ROOT_PASSWORD:?Set POSTGRES_ROOT_PASSWORD in .env.production}
    command: ["postgres", "-c", "ssl=on", "-c", "ssl_cert_file=/etc/postgresql/tls/server.crt", "-c", "ssl_key_file=/etc/postgresql/tls/server.key", "-c", "hba_file=/etc/postgresql/pg_hba.conf"]
    volumes:
      - ./db-certs:/etc/postgresql/tls:ro
      - ../postgres/pg_hba.prod.conf:/etc/postgresql/pg_hba.conf:ro
      - postgres-data:/var/lib/postgresql
    networks:
      - kredit-backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U kredit -d kredit"]
      interval: 5s
      timeout: 5s
      retries: 12

  migrate:
    build:
      context: ../..
      dockerfile: infra/containers/Dockerfile.api
      target: api
    entrypoint: ["/app/kredit-migrate"]
    environment:
      DATABASE_URL: postgres://kredit:${POSTGRES_ROOT_PASSWORD:?Set POSTGRES_ROOT_PASSWORD in .env.production}@postgres:5432/kredit?sslmode=verify-full&sslrootcert=/etc/postgresql/root.crt
    volumes:
      - ./db-certs/root.crt:/etc/postgresql/root.crt:ro
    networks:
      - kredit-backend
    depends_on:
      postgres:
        condition: service_healthy

  role-init:
    image: postgres:18
    entrypoint: ["/bin/sh", "-c"]
    command:
      - >-
        psql "$${DATABASE_DIRECT_URL}" -X -v ON_ERROR_STOP=1 -f /config/roles.sql &&
        psql "$${DATABASE_DIRECT_URL}" -X -v ON_ERROR_STOP=1
        -v app_login_password="$${KREDIT_APP_DB_PASSWORD:?Set KREDIT_APP_DB_PASSWORD in .env.production}"
        -v worker_login_password="$${KREDIT_WORKER_DB_PASSWORD:?Set KREDIT_WORKER_DB_PASSWORD in .env.production}"
        -v backup_login_password="$${KREDIT_BACKUP_DB_PASSWORD:?Set KREDIT_BACKUP_DB_PASSWORD in .env.production}"
        -v demote_owner=false
        -f /config/development-logins.sql
    environment:
      DATABASE_DIRECT_URL: postgres://kredit:${POSTGRES_ROOT_PASSWORD:?Set POSTGRES_ROOT_PASSWORD in .env.production}@postgres:5432/kredit?sslmode=verify-full&sslrootcert=/etc/postgresql/root.crt
      KREDIT_APP_DB_PASSWORD: ${KREDIT_APP_DB_PASSWORD:?Set KREDIT_APP_DB_PASSWORD in .env.production}
      KREDIT_WORKER_DB_PASSWORD: ${KREDIT_WORKER_DB_PASSWORD:?Set KREDIT_WORKER_DB_PASSWORD in .env.production}
      KREDIT_BACKUP_DB_PASSWORD: ${KREDIT_BACKUP_DB_PASSWORD:?Set KREDIT_BACKUP_DB_PASSWORD in .env.production}
    volumes:
      - ../postgres:/config:ro
      - ./db-certs/root.crt:/etc/postgresql/root.crt:ro
    networks:
      - kredit-backend
    depends_on:
      migrate:
        condition: service_completed_successfully

  api:
    build:
      context: ../..
      dockerfile: infra/containers/Dockerfile.api
      target: api
    restart: unless-stopped
    env_file:
      - .env.runtime
    environment:
      ADMIN_CONFIG_AUTO_APPLY: "true"
      API_ADDR: :8080
      DATABASE_URL: postgres://kredit_app_login:${KREDIT_APP_DB_PASSWORD:?Set KREDIT_APP_DB_PASSWORD in .env.production}@postgres:5432/kredit?sslmode=verify-full&sslrootcert=/etc/postgresql/root.crt
      RIVER_DATABASE_URL: postgres://kredit_worker_login:${KREDIT_WORKER_DB_PASSWORD:?Set KREDIT_WORKER_DB_PASSWORD in .env.production}@postgres:5432/kredit?sslmode=verify-full&sslrootcert=/etc/postgresql/root.crt
    volumes:
      - ./db-certs/root.crt:/etc/postgresql/root.crt:ro
    networks:
      - kredit-backend
    healthcheck:
      test: ["CMD", "/app/kredit-api", "-healthcheck"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 15s
    depends_on:
      role-init:
        condition: service_completed_successfully

  worker:
    build:
      context: ../..
      dockerfile: infra/containers/Dockerfile.api
      target: worker
    restart: unless-stopped
    env_file:
      - .env.runtime
    environment:
      ADMIN_CONFIG_AUTO_APPLY: "true"
      DATABASE_URL: postgres://kredit_app_login:${KREDIT_APP_DB_PASSWORD:?Set KREDIT_APP_DB_PASSWORD in .env.production}@postgres:5432/kredit?sslmode=verify-full&sslrootcert=/etc/postgresql/root.crt
      RIVER_DATABASE_URL: postgres://kredit_worker_login:${KREDIT_WORKER_DB_PASSWORD:?Set KREDIT_WORKER_DB_PASSWORD in .env.production}@postgres:5432/kredit?sslmode=verify-full&sslrootcert=/etc/postgresql/root.crt
    volumes:
      - ./db-certs/root.crt:/etc/postgresql/root.crt:ro
    networks:
      - kredit-backend
    healthcheck:
      test: ["CMD", "/app/kredit-worker", "-healthcheck"]
      interval: 15s
      timeout: 5s
      retries: 4
      start_period: 15s
    depends_on:
      role-init:
        condition: service_completed_successfully

  caddy:
    image: caddy:2.9-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile.prod:/etc/caddy/Caddyfile:ro
      - ./certs:/etc/caddy/certs:ro
      - caddy-data:/data
      - caddy-config:/config
    networks:
      - kredit-backend
    depends_on:
      api:
        condition: service_healthy

volumes:
  postgres-data:
  caddy-data:
  caddy-config:

networks:
  kredit-backend:
    driver: bridge
