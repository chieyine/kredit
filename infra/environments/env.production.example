# Deployment-only credentials. Copy to .env.production.
# Compose interpolation reads this file; API/worker do not receive it.
# Use URL-safe random hex passwords (openssl rand -hex 32).
POSTGRES_ROOT_PASSWORD=
KREDIT_APP_DB_PASSWORD=
KREDIT_WORKER_DB_PASSWORD=
KREDIT_BACKUP_DB_PASSWORD=
