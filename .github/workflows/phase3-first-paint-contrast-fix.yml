name: Phase 3 First Paint Contrast Fix

on:
  push:
    branches: [phase3-financial-proof]
    paths: ['.github/workflows/phase3-first-paint-contrast-fix.yml']

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.17.0
      - uses: actions/setup-node@v4
        with:
          node-version-file: .node-version
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm --dir web exec playwright install --with-deps chromium
      - name: Apply and verify first-paint contrast fix
        run: |
          python3 scripts/fix_phase3_first_paint_contrast.py
          pnpm --dir web run check
          pnpm --dir web exec playwright test tests/product-quality.spec.ts --project=chromium
      - name: Commit verified UI fix
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add web/src/routes/demo/+page.svelte
          git diff --cached --check
          git commit -m 'fix: keep demo contrast valid before hydration'
          git push
