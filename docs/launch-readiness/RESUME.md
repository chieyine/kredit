# Resume from this local checkpoint

Read OPERATING-NOTES.md and the replacement master prompt. Do not treat this checkpoint as
completion. Preserve its changes and the original brief. No remote repository is needed.

Start with the real local Node preview and the isolated PostgreSQL database. Finish the
remaining core supplier/buyer journeys and owner settings-to-runtime-to-worker connections,
then the full route/state design sweep. The current public fixes are verified only for the
routes and viewports recorded in evidence. No authenticated screenshots were captured.

High-priority unresolved work:
- Owner-managed legal/content publication instead of the initial code-backed publication.
- Provider credentials/mode consumption and rotation/reconciliation across API and workers.
- Settings group revisions/rollback/idempotent replay and measured provider verification.
- Owner lifecycle last-owner concurrency, transfer, suspension, expiry and secure recovery.
- Optional-provider production startup separation and genuinely independent customer flows.
- Full real database/worker/payment/browser/restore/load/security acceptance matrix.
- Resolve the local Go race runtime issue and use the project's supported Node >=24.20;
  this task's Node was 24.11.0, pnpm 11.17.0, Go 1.27.1, PostgreSQL 18.

Do not replace those tasks with approval requests, hide core features, or describe a saved
configuration value as effective. The owner has already authorised the local work.
