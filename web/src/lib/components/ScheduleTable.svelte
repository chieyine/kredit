<script lang="ts">import Money from './Money.svelte'; import StatusPill from './StatusPill.svelte'; let { items = [] } = $props<{ items?: Array<{ sequence: number; due: string; amountKobo: number; status: string }> }>();</script>
<div class="table-wrap"><table><caption class="sr-only">Payment dates</caption><thead><tr><th scope="col">Payment</th><th scope="col">Pay before</th><th scope="col">Money to pay</th><th scope="col">Now</th></tr></thead><tbody>{#each items as item}<tr><td>{item.sequence}</td><td>{item.due}</td><td><Money amountKobo={item.amountKobo} /></td><td><StatusPill status={item.status} /></td></tr>{/each}</tbody></table></div>
