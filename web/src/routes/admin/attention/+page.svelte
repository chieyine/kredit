<script lang="ts">
 import {onMount} from 'svelte';import AdminAttention from '$lib/components/AdminAttention.svelte';import {adminGet,localTime} from '$lib/admin-client';
 let data:any=$state(null),error=$state('');onMount(async()=>{try{data=await adminGet('/api/v1/ops/attention/details')}catch(e){error=String(e)}});
</script>
<svelte:head><title>Work needing attention — Kredit</title></svelte:head>
<main class="shell workspace"><h1>Work needing attention</h1><AdminAttention/>{#if error}<p role="alert">{error}</p>{/if}{#if data}<p>Shows up to 100 records in each category. Resolve existing cases through their recorded workflows.</p><h2>Mandates approaching expiry</h2>{#each data.mandates as item}<article><h3>{item.buyer}</h3><p>Expires {localTime(item.expires_at)} · Reference {item.id}</p><p>Ask the buyer to review their bank debit authorization in their account. Renewed provider authorization is required.</p><a href={`/admin/users?q=${encodeURIComponent(item.buyer_id||'')}`}>Review buyer account →</a></article>{:else}<p>No mandates approaching expiry.</p>{/each}<h2>Uncertain debits</h2>{#each data.debits as item}<article><p>{item.state} since {localTime(item.requested_at)} · Reference {item.id}</p><a href={`/admin/controls?target_type=collection&target_id=${item.id}`}>Look up this debit →</a></article>{:else}<p>No uncertain debits.</p>{/each}<h2>Failed notices</h2>{#each data.notices as item}<article><h3>{item.recipient} · {item.template}</h3><p>{item.channel} · {item.reason||'Delivery failed'} · Reference {item.id}</p><a href="/admin/diagnostics">Review delivery processing →</a></article>{:else}<p>No failed notices.</p>{/each}{/if}</main>
<style>article{padding:1rem;margin:1rem 0;border:1px solid #bbb6ac;background:#fffdf6}p{overflow-wrap:anywhere}</style>
