<script lang="ts">import { page } from '$app/state';import DisputeDetail from '$lib/components/DisputeDetail.svelte';let organizationID=$derived(page.url.searchParams.get('organization')??'');</script>
<svelte:head><title>Dispute status — Kredit</title></svelte:head><main class="shell workspace"><p class="eyebrow">Supplier / Dispute</p><h1>Evidence, status and decision.</h1>{#if organizationID}<DisputeDetail endpoint={`/api/v1/organizations/${organizationID}/disputes/${page.params.id}`} backHref="/app/disputes" canDecide />{:else}<p class="error" role="alert">The organisation context is missing. Return to the disputes list and open this record again.</p>{/if}</main>
