<script lang="ts">import { page } from '$app/state';import DisputeDetail from '$lib/components/DisputeDetail.svelte';let organizationID=$derived(page.url.searchParams.get('organization')??'');</script>
<svelte:head><title>Problem details — Kredit</title></svelte:head><main class="shell workspace"><p class="eyebrow">Reported problem</p><h1>See what happened and provide evidence.</h1>{#if organizationID}<DisputeDetail endpoint={`/api/v1/organizations/${organizationID}/disputes/${page.params.id}`} backHref="/app/disputes" />{:else}<p class="error" role="alert">We could not find the business for this problem. Go back to the problems page and open it again.</p>{/if}</main>
